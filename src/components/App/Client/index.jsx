import React, { memo } from 'react';

function AppClient() {
  /* Global & local State */
  /* Functions */
  /* Views */
  /* Main */
  return (
    <Box>
      123
    </Box>
  );
}

export default memo(AppClient);
